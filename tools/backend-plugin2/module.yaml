product: jvm/amper-plugin

dependencies:
  - ../processor-common
  - ../gradle-plugins/application
  - ../../common/kobweb-common # probably should refactor out the necessary part to a separate module

settings:
  kotlin:
    serialization:
      enabled: true
      format: json

apply:
  - ../../common.module-template.yaml

pluginInfo:
  settingsClass: StartSettings
